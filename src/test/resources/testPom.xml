<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- The Basics -->
    <groupId>net.kindleit</groupId>
    <artifactId>maven-gae-example</artifactId>
    <version>1.0-SNAPSHOT</version>

    <name>maven-gae-example</name>
    <description>Google AppEngine project example</description>
    <packaging>war</packaging>

    <build>
        <plugins>
            <plugin>
                <groupId>net.kindleit</groupId>
                <artifactId>maven-gae-plugin</artifactId>
                <version>0.4-SNAPSHOT</version>
                <configuration>
                  <project implementation="net.kindleit.gae.testUtils.GoalProjectStub"/>
                  <sdkDir>${gae.home}</sdkDir>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!-- Specify hard-coded project properties here -->
    <properties>
        <!-- This is just for "eclipse:eclipse" goal to always attempt downloading sources  -->
        <downloadSources>true</downloadSources>

        <!--
            Specify AppEngine version for your project. 
            It should match SDK version pointed to by ${gae.home} property
            (Typically, one used by your Eclipse plug-in)
        -->
        <gae.version>1.2.1</gae.version>

        <!--
            GAE home points to directory where GAE SDK is unpacked in local repository 
        -->
        <gae.home>
            ${settings.localRepository}/com/google/appengine/appengine-java-sdk/${gae.version}/appengine-java-sdk-${gae.version}
        </gae.home>
        
        <!-- GWT version -->
        <gwt.version>1.6.3</gwt.version>
        
        <!--
          platform is auto-selected in profiles section, but we provide default value here,
          in case this fails.
         -->
        <platform>linux</platform>

        <!--
          GWT has issues with running on 64-bit Java, so we use special java32.home property for it. 
          By default it points to JAVA_HOME, but you must override this property
          in your maven settings.xml, if your JAVA_HOME points to 64-bit Java.
         -->
        <java32.home>${JAVA_HOME}</java32.home>
    </properties>
</project>